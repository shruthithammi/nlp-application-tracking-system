# Database Schema

`users` -> candidate accounts

`applications` -> company, role, status and explanation

`application_events` -> chronological timeline

`recruiter_emails` -> analyzed recruiter messages and classifier results

`notifications` -> candidate-facing updates
